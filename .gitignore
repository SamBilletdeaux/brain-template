# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/

# Inbox — raw data, not committed (snapshotted transcripts, uploads, notes)
inbox/

# Search index — derived from markdown files, rebuildable
.brain.db
.brain.db-wal
.brain.db-shm

# Wind-down checkpoint (session state, not data)
.wind-down-checkpoint.json

# Session lockfile
.brain.lock

# Don't ignore any markdown files — they're the whole point
